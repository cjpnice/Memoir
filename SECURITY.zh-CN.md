# 安全策略

[English](SECURITY.md)

Memoir / 集忆 当前是本地优先、单用户、pre-1.0 项目。导入照片、生成相册、AI 设置、API Key 和 `DATA_DIR` 下的数据都应视为用户私有数据。

## 支持范围

pre-1.0 阶段仅维护当前 `main` 分支。

## 报告漏洞

如果仓库托管在 GitHub，请优先使用 GitHub Security Advisory 私密报告漏洞。如果该入口尚未启用，请先联系项目维护者，不要公开披露可利用细节。

报告时请包含：

- 问题简述。
- 复现步骤。
- 是否可能暴露照片、导出相册、分享页、API Key 或本地文件。
- 操作系统、浏览器、运行方式、本地/Docker/单文件模式。
- 是否使用了 AI 供应商，以及供应商类型。

不要上传私人照片或真实 API Key。请尽量使用脱敏或可公开授权的素材复现。

## 当前安全边界

- Memoir 没有内置用户认证，不应直接暴露到公网。
- 默认配置面向本地开发。公开部署前必须增加认证、TLS、访问控制和精确 CORS 来源。
- Docker Compose 适合本地或可信网络，不是开箱即用的公网 SaaS 部署方案。
- 单文件应用仍然会启动本地 Web 服务；如果监听地址被改到公网网卡，也会产生同样的暴露风险。
- 配置 AI 供应商后，上传图片内容会发送给该供应商。隐私敏感照片请使用自托管网关。
- `DATA_DIR` 保存项目状态、媒体文件、导出内容和应用内 AI 设置。请为该目录设置合适的本机权限和备份策略。

## 不应提交的内容

- `.env`
- `data/`
- `dist/`
- `samples/demo-photos/`
- `images/`
- 真实照片或生成的私人相册
- API Key、访问令牌或供应商密钥

## 安全改进方向

- 明确的登录与分享权限模型。
- 公开部署反向代理示例。
- 更细的导出分享访问控制。
- 可选数据库和对象存储适配器。
- 前端交互与上传边界测试。
